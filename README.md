# Wind Turbine Sound Generator (Web Audio)

Adds wind direction visualization and acoustic effect:
- **Toward listener**: +20% amplitude, +5 kHz LPF cutoff
- **Away from listener**: −20% amplitude, −5 kHz LPF cutoff
- **Neutral**: baseline

See `app.js` → `applyParams()` and `drawWindIndicator()`.
